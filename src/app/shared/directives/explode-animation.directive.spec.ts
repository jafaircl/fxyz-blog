import { ExplodeAnimationDirective } from './explode-animation.directive';

describe('ExplodeAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new ExplodeAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
